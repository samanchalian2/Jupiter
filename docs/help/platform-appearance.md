---
slug: platform-appearance
title: رنگ اصلی و ظاهر Platform و سازمان
summary: رنگ پایهٔ ژوپیتر، Custom Primary کنترل‌شده و تفاوت تنظیم پلتفرم با سازمان.
category: مدیریت Platform
audience: ["PLATFORM_ADMIN", "ORG_ADMIN", "ORG_OWNER"]
tags: ["ظاهر", "Appearance", "لوگو", "برند", "Platform", "سازمان"]
productArea: ظاهر
relatedFeature: PLATFORM_APPEARANCE
relatedRoute: /platform/appearance
---

# رنگ اصلی و ظاهر Platform و سازمان

رنگ پایهٔ Jupiter در Light Theme، `#014348` است. خانوادهٔ پیش‌فرض آن hover `#00383C`، active `#002D31`، سطح ملایم `#EFF7F7` و border تأکیدی `#BFD9D9` دارد. Platform Admin می‌تواند presetهای کنترل‌شده، رنگ اصلی سفارشی، تراکم، radius و لوگوی داخلی را مدیریت کند. رنگ سفارشی فقط خانوادهٔ رنگ اصلی را تغییر می‌دهد؛ رنگ‌های موفقیت، هشدار، خطا و عملیات مخرب مستقل می‌مانند.

رنگ سفارشی فقط با قالب `#RRGGBB` پذیرفته می‌شود؛ برای نمونه `#014348` یا `#1A6F55`. کد CSS، `rgb(...)`، نام رنگ، متغیر، URL، gradient یا محتوای دلخواه قابل ذخیره نیست. قبل از ذخیره، پیش‌نمایش دکمه، پیوند، ناوبری انتخاب‌شده، focus و نمونهٔ رنگ‌های معنایی را بررسی کنید. سرور نیز کنتراست را دوباره بررسی می‌کند و برای متن روی دکمه، foreground سفید یا تیره را به‌صورت deterministic انتخاب می‌کند.

اگر سازمان رنگ سفارشی نداشته باشد، رنگ مؤثر Platform را ارث می‌برد. Platformهایی که رنگ سفارشی یا preset غیرپیش‌فرض ندارند، رنگ `#014348` را ارائه می‌کنند. «حذف رنگ سفارشی» در سازمان فقط override سازمان را حذف می‌کند؛ در Platform نیز فقط `custom_primary` را حذف می‌کند و preset انتخاب‌شده، تراکم، radius و لوگو را نگه می‌دارد. بنابراین پس از حذف، رنگ مؤثر از preset فعلی می‌آید: ژوپیتر `#014348`، آبی متین یا سبز نفتی. در صورت افزودن بازنشانی کامل به ژوپیتر در آینده، باید یک عملیات جداگانه باشد و با حذف رنگ سفارشی یکی نیست.

سازمان می‌تواند رنگ اصلی و لوگوی خود را در محدودهٔ مجاز نمایش دهد، اما نمی‌تواند رنگ‌های معنایی، layout یا ناوبری پلتفرم را بازتعریف کند. تغییر ظاهر باید پیش‌نمایش و خوانایی فارسی و RTL را حفظ کند.

Appearance مسیر تغییر نقش، دسترسی، دادهٔ سازمان یا قرارداد تجاری نیست. برای مشکل بصری پایدار، تنظیم را به حالت مناسب بازگردانید و با Platform Admin پیگیری کنید.
