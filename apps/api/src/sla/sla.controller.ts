import { Body, Controller, Get, Headers, Post } from '@nestjs/common';
import { TicketActorService } from '../tickets/ticket-actor.service.js';
import { SlaService } from './sla.service.js';
@Controller('sla')
export class SlaController { constructor(private readonly actors:TicketActorService,private readonly sla:SlaService) {} private actor(a?:string,o?:string){return this.actors.fromHeaders(a,o);} @Get('policies') policies(@Headers('authorization') a?:string,@Headers('x-organization-id') o?:string){return this.actor(a,o).then(actor=>this.sla.policies(actor));} @Post('policies') save(@Body() body:{name:string;priority:'LOW'|'NORMAL'|'HIGH'|'URGENT';firstResponseMinutes:number;resolutionMinutes:number;warningMinutes:number;escalationRole:'SUPERVISOR'|'ORG_ADMIN';isActive?:boolean},@Headers('authorization') a?:string,@Headers('x-organization-id') o?:string){return this.actor(a,o).then(actor=>this.sla.savePolicy(actor,body));} @Get('calendar') calendar(@Headers('authorization') a?:string,@Headers('x-organization-id') o?:string){return this.actor(a,o).then(actor=>this.sla.calendar(actor));} @Post('calendar') saveCalendar(@Body() body:{timezone:string;workdays:number[];startMinute:number;endMinute:number},@Headers('authorization') a?:string,@Headers('x-organization-id') o?:string){return this.actor(a,o).then(actor=>this.sla.saveCalendar(actor,body));} @Get('clocks') clocks(@Headers('authorization') a?:string,@Headers('x-organization-id') o?:string){return this.actor(a,o).then(actor=>this.sla.clocks(actor));} @Get('rules') rules(@Headers('authorization') a?:string,@Headers('x-organization-id') o?:string){return this.actor(a,o).then(actor=>this.sla.rules(actor));} @Post('rules') rule(@Body() body:{departmentId?:string;assigneeUserId:string;isActive?:boolean},@Headers('authorization') a?:string,@Headers('x-organization-id') o?:string){return this.actor(a,o).then(actor=>this.sla.saveRule(actor,body));} @Get('preferences') preference(@Headers('authorization') a?:string,@Headers('x-organization-id') o?:string){return this.actor(a,o).then(actor=>this.sla.preference(actor));} @Post('preferences') preferenceSave(@Body() body:{inAppEnabled:boolean},@Headers('authorization') a?:string,@Headers('x-organization-id') o?:string){return this.actor(a,o).then(actor=>this.sla.savePreference(actor,body.inAppEnabled));} }
