import { Body, Controller, Get, Headers, Param, Post, Query } from '@nestjs/common'; import { TicketActorService } from '../tickets/ticket-actor.service.js'; import { KnowledgeService } from './knowledge.service.js'; @Controller('knowledge') export class KnowledgeController {constructor(private readonly actors:TicketActorService,private readonly knowledge:KnowledgeService){} @Get() list(@Query('q')q:string,@Headers('authorization')a?:string,@Headers('x-organization-id')o?:string){return this.actors.fromHeaders(a,o).then(x=>this.knowledge.list(x,q??''))} @Post() create(@Body()b:{title:string;body:string},@Headers('authorization')a?:string,@Headers('x-organization-id')o?:string){return this.actors.fromHeaders(a,o).then(x=>this.knowledge.create(x,b))} @Post(':id/publish') publish(@Param('id')id:string,@Headers('authorization')a?:string,@Headers('x-organization-id')o?:string){return this.actors.fromHeaders(a,o).then(x=>this.knowledge.publish(x,id))}}
