import { ForbiddenException, Injectable } from '@nestjs/common'; import { DatabaseService } from '../database/database.service.js'; type Actor={userId:string;organizationId:string;roles:string[]}; @Injectable() export class KnowledgeService {constructor(private readonly database:DatabaseService){} async list(actor:Actor,q=''){return this.database.withOrganization(actor.organizationId,async c=>(await c.query('SELECT id,title,body,status,updated_at FROM knowledge_articles WHERE status=\'PUBLISHED\' AND (title ILIKE $1 OR body ILIKE $1) ORDER BY updated_at DESC',[`%${q}%`])).rows)} async create(actor:Actor,input:{title:string;body:string}){if(!actor.roles.some(r=>['ORG_ADMIN','SUPERVISOR','EXPERT'].includes(r)))throw new ForbiddenException();return this.database.withOrganization(actor.organizationId,async c=>(await c.query('INSERT INTO knowledge_articles(organization_id,title,body,author_user_id) VALUES($1,$2,$3,$4) RETURNING id,title,status',[actor.organizationId,input.title,input.body,actor.userId])).rows[0])} async publish(actor:Actor,id:string){if(!actor.roles.some(r=>['ORG_ADMIN','SUPERVISOR'].includes(r)))throw new ForbiddenException();return this.database.withOrganization(actor.organizationId,async c=>(await c.query('UPDATE knowledge_articles SET status=\'PUBLISHED\',reviewer_user_id=$1,published_at=now(),updated_at=now() WHERE id=$2 RETURNING id,status',[actor.userId,id])).rows[0])}}
