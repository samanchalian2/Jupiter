{
  "name": "jupiter",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "node scripts/dev-all.mjs",
    "build": "pnpm -r build",
    "dev:api": "pnpm --filter @jupiter/api dev",
    "dev:web": "pnpm --filter @jupiter/web dev",
    "dev:worker": "pnpm --filter @jupiter/api worker",
    "dev:storage": "node apps/api/scripts/local-storage.mjs",
    "dev:ai-fake": "node scripts/fake-openai-provider.mjs",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "verify:release": "pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "load:smoke": "node scripts/load-smoke.mjs"
  }
}
