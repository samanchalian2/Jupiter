# Event Catalog

MVP events: `TicketDraftCreated`, `VoiceUploaded`,
`VoiceTranscriptionRequested`, `VoiceTranscribed`, `VoiceTranscriptionFailed`,
`TicketAIAnalysisRequested`, `TicketAIAnalyzed`, `TicketAIAnalysisFailed`,
`TicketSubmitted`, `TicketAssigned`, `TicketReassigned`, `TicketStatusChanged`,
`UserReplied`, `ExpertReplied`, `AIReplied`, `InternalNoteAdded`,
`TicketResolved`, `TicketClosed`, `TicketReopened`, and `TicketRated`.

Future-only: duplicate detection, SLA escalation, knowledge candidate/article,
and advanced reporting events.
